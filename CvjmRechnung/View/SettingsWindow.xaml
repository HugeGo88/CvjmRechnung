<Window x:Class="CvjmRechnung.View.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Einstellungen"
        Height="300"
        Width="520"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="E-Mail Passwort" Margin="0,0,0,6" />
        <PasswordBox Grid.Row="1" x:Name="PasswordBox" />

        <TextBlock Grid.Row="2" Text="Rechnungsordner" Margin="0,12,0,6" />
        <TextBox Grid.Row="3" x:Name="InvoicePathTextBox" />

        <TextBlock Grid.Row="4" Text="CSS-Stil fuer PDF" Margin="0,12,0,6" />
        <ComboBox Grid.Row="5"
                  x:Name="CssFileComboBox"
                  ToolTip="CSS-Datei aus %appdata%\CvjmRechnung\css auswaehlen" />

        <StackPanel Grid.Row="7"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Speichern"
                    Width="100"
                    Margin="0,0,8,0"
                    Click="Save_Click" />
            <Button Content="Abbrechen"
                    Width="100"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>